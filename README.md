# Getting started

### Build&Run:
``docker compose up frontend -d``
### Stop:
``docker rm -f frontend``
### Rebuilding
```docker image ls```

``docker rmi imageId``

``docker compose up frontend -d``
